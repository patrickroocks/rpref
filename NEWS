rPref 0.3
=========


* Translation of preferences to other preference query languages via `show.query`

* Optimized evaluation of preferences with long prioritization chains

* Added neutral element `empty()` for preference composition

* Fixed memory leak in preference evaulation

* Fixed bug: wrong Hasse diagramms for some kinds of preference in get_btg

* Fixed bug: psel on empty dataframes

* Hasse diagramm can be returned as adjacency list via `get_hasse_diag()`