rPref 0.5
=========

* New functions for string output (pref.str) and partial evaluation (eval.pref)

* Predecessor/successor (hasse_pred, etc.) result vectors are ordered ascending

* New functionality predecessor/successor functions (support empty vectors, new parameter "intersect")

* New base preferences for defining preferences on expressions/symbols (low_, high_, true_)

* Fixed bug in multihtreaded calculation of preferences